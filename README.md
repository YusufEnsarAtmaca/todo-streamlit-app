# 📝 Streamlit Todo List with Docker

This project is a simple Todo List application built with **Python**, **Streamlit**, and **PostgreSQL**, containerized using **Docker**.

## 🚀 How to Run

1. **Prerequisites:** Make sure you have [Docker Desktop](https://www.docker.com/products/docker-desktop/) installed.
2. **Clone the project:**
   ```bash
   git clone <YOUR_GITHUB_REPO_URL>
   cd "Docker hw"